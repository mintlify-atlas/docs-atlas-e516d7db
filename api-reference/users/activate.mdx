---
title: 'Activate user'
description: 'Reactivate a deactivated user account.'
---

`POST /api/users/{user_id}/activate`

Sets a user's `is_active` status to `true`, restoring their ability to authenticate and use the API.

<Note>
  **Admin only.** This endpoint requires the `admin` role.
</Note>

## Path parameters

<ParamField path="user_id" type="integer" required>
  The unique identifier of the user to activate.
</ParamField>

## Response fields

<ResponseField name="success" type="boolean" required>
  Whether the request succeeded.
</ResponseField>

<ResponseField name="message" type="string">
  Confirmation message.
</ResponseField>

<ResponseField name="data" type="object" required>
  The updated user object with `is_active` set to `true`.
  <Expandable title="properties">
    <ResponseField name="id" type="integer">
      Unique identifier for the user.
    </ResponseField>
    <ResponseField name="username" type="string">
      Unique username.
    </ResponseField>
    <ResponseField name="email" type="string">
      User's email address.
    </ResponseField>
    <ResponseField name="first_name" type="string">
      User's first name.
    </ResponseField>
    <ResponseField name="last_name" type="string">
      User's last name.
    </ResponseField>
    <ResponseField name="full_name" type="string">
      Computed full name.
    </ResponseField>
    <ResponseField name="is_active" type="boolean">
      Whether the user account is active. Will be `true` after a successful activation.
    </ResponseField>
    <ResponseField name="role" type="object">
      The user's assigned role.
      <Expandable title="properties">
        <ResponseField name="id" type="integer">
          Role ID.
        </ResponseField>
        <ResponseField name="name" type="string">
          Role name: `admin` or `user`.
        </ResponseField>
        <ResponseField name="description" type="string">
          Role description.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="created_at" type="string">
      ISO 8601 timestamp when the user was created.
    </ResponseField>
    <ResponseField name="updated_at" type="string">
      ISO 8601 timestamp when the user was last updated.
    </ResponseField>
  </Expandable>
</ResponseField>

<RequestExample>

```bash cURL
curl --request POST \
  --url 'https://api.taskforge.com/api/users/1/activate' \
  --header 'Authorization: Bearer <access_token>'
```

</RequestExample>

<ResponseExample>

```json 200
{
  "success": true,
  "message": "Usuario activado con exito",
  "data": {
    "id": 1,
    "username": "johndoe",
    "email": "john@example.com",
    "first_name": "John",
    "last_name": "Doe",
    "full_name": "John Doe",
    "is_active": true,
    "role": {
      "id": 2,
      "name": "user",
      "description": "Regular user"
    },
    "created_at": "2024-01-01T00:00:00",
    "updated_at": "2024-01-15T10:00:00"
  }
}
```

```json 403
{
  "success": false,
  "error": "Acceso de administrador requerido"
}
```

```json 404
{
  "success": false,
  "error": "Usuario no encontrado"
}
```

</ResponseExample>
